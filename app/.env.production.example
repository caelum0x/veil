# Copy to .env.production and point at your deployed services before `npm run build`.
# These are baked into the bundle at build time.
VITE_RELAYER_URL=https://relayer.your-domain.example
VITE_INDEXER_URL=https://indexer.your-domain.example
VITE_RPC_URL=https://soroban-testnet.stellar.org
